```
├── rust -> add rust overlay to provide custom Rust toolchains and tools
├── additions -> add custom packages
├── stable -> expose a stable channel
└── flake-inputs -> provide aliases for flake inputs for current system
```
